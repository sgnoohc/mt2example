    git clone git@github.com:sgnoohc/mt2example.git
    cd mt2example
    # modify the main.cc with the input event
    source setuproot.sh # assumes the system has cvmfs setup
    make
    ./test_mt2
